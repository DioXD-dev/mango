<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Compose handles all theming, this just sets the window background -->
    <style name="Theme.MangoPlayer" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
